import { updateData, updateDimensions, updateCurrentDatum, driveChartUpdate, generateChart } from './dataPane';

export {
    updateData,
    updateDimensions,
    updateCurrentDatum,
    driveChartUpdate,
    generateChart
};